# include <fstream>
# include <iostream>
# include <vector>

using namespace std;

int analyze_data(ifstream& file)
{
    string line;
    vector<int> Id, Marker;
    vector<double> X,Y;

    getline(file, line);

    while(getline(file, line))
    {
        
    }

    return 0;
}